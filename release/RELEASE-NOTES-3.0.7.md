# StepDaddy Gateway 3.0.7

## Special Events cap fixes

- Max 2 upstream links per schedule event (primary + backup)
- Live/on-air events prioritized by start time instead of category A–Z
- Expired rows pruned before merge refresh
- Stream cap raised 60 → 120

Sideload `stepdaddy-gateway-3.0.7-release.apk` (`com.thothassistant.stepdaddy.gateway`).
