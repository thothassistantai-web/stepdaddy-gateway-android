# StepDaddy Gateway 3.0.11

## Added

- **Volume normalization** — Settings → Audio toggle requests loudness normalization from TiviMate and StreamVault (player-side when supported)
- **Amplification gain** — Settings → Audio slider (−12 to +12 dB) for the embedded dashboard/fullscreen preview player
- **API exposure** — `/health`, `/tivimate-setup`, `/streamvault-setup`, and StreamVault `playback.prepare` include `audio` / `audio_json` metadata

Sideload `stepdaddy-gateway-3.0.11-release.apk` (`com.thothassistant.stepdaddy.gateway`).
