# StepDaddy Gateway 3.0.30

versionCode: 30030

- **Domain relay** — Temporary DaddyLive host overlay via `domain-relay.json` (banner + AFTV codes when active).
- **VOD catalog relay** — Live movie/show finds via `vod-catalog-relay.json` with dedupe and dead-link probe.
